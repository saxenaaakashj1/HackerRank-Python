# Read an integer input from the user and assign it to variable 'a'
a = int(input())

# Read an integer input from the user and assign it to variable 'b'
b = int(input())

# Read an integer input from the user and assign it to variable 'c'
c = int(input())

# Read an integer input from the user and assign it to variable 'd'
d = int(input())

# Calculate a raised to the power of b and c raised to the power of d,
# then add the two results, and print
print(pow(a, b) + pow(c, d))
